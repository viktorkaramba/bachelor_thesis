import {MaintenanceCostsCars} from './maintenancecostscars';

describe('MaintenanceCostsCars', () => {
  it('should create an instance', () => {
    expect(new MaintenanceCostsCars()).toBeTruthy();
  });
});
