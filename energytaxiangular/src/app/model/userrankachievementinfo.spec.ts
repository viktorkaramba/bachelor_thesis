import { UserRankAchievementInfo } from './userrankachievementinfo';

describe('UserRankAchievementInfo', () => {
  it('should create an instance', () => {
    expect(new UserRankAchievementInfo()).toBeTruthy();
  });
});
