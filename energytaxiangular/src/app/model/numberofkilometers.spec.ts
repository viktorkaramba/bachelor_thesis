import {NumberOfKilometers} from './numberofkilometers';

describe('NumberOfKilometers', () => {
  it('should create an instance', () => {
    expect(new NumberOfKilometers()).toBeTruthy();
  });
});
